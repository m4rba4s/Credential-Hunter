## Summary
Briefly describe the problem and the solution.

## Problem / Motivation
Why is this change needed? Link issues (e.g., #123).

## Changes
- High-level bullets of what changed
- Note modules touched (e.g., `src/core/engine.rs`)

## How to Test
Commands and expected results:

```bash
scripts/test
scripts/format
scripts/lint
cargo run --bin ech -- file-scan --target /tmp
```

## Feature Flags / Config
List required features (e.g., `--features simd-optimizations`) and config (e.g., `--config path`).

## Security Considerations
Note any privileged operations (`--privileged`), sensitive data handling, or risk areas.

## Screenshots / Logs
Attach screenshots or log snippets if UI/CLI output changed.

## Checklist
- [ ] Tests added/updated and pass (`scripts/test`)
- [ ] Code formatted (`scripts/format`) and linted (`scripts/lint`)
- [ ] Docs updated (README/ARCHITECTURE.md/AGENTS.md where relevant)
- [ ] No secrets or sensitive data committed
- [ ] Linked related issue(s)

